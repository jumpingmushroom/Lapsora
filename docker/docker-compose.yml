services:
  lapsora:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - lapsora-data:/app/data
    env_file:
      - ../.env
    restart: unless-stopped

volumes:
  lapsora-data:
